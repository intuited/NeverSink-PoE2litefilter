#!/bin/bash
# Update script for WSL2
# Assumes that you've got a symlink at ~/home pointing to your Windows home directory.
# You probably don't, so maybe just ignore this.
cp -uv NeverSinks\ Litefilter.filter ~/home/Documents/My\ Games/Path\ of\ Exile\ 2/custom.filter
